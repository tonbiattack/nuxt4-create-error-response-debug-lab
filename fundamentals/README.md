# Fundamentals

このディレクトリには、失敗するHTTP契約を観測してから、最小修正と回帰確認へ進む章別ガイドを置きます。

| 章 | 題材 | 状態 |
| --- | --- | --- |
| [第01章](01-create-error-response.md) | エラーJSONを`return`してHTTP `200`になる問題 | 実装済み |

各章では、バグ導入コミットでRedを再現し、修正コミットまたは`main`で同じテストをGreenにします。原因を先に読みたくない場合は、章ガイドのRed節だけを実行してください。
